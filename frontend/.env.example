# Backend API base URL (proxied via Vite in dev, nginx in prod)
VITE_API_URL=/api

# Set this ONLY if you want the frontend to call Anthropic directly
# (used by AICopilot when backend is not available)
# VITE_ANTHROPIC_KEY=your-key-here
